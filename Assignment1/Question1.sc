// Type inference for variable declaration
val myNumber = 42 // Compiler infers that myNumber is of type Int

// Type inference for function return type
def add(x: Int, y: Int) = x + y // Compiler infers that the return type is Int

// Type inference in collections
val myList = List(1, 2, 3, 4, 5) // Compiler infers that myList is of type List[Int]

// Type inference with generics
val myPair = (42, "Scala") // Compiler infers that myPair is of type (Int, String)
